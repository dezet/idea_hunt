import "reflect-metadata"
import 'mocha'
import * as chai from 'chai'
import { setupChai } from './utils/utils'

const expect = setupChai(chai)
describe('ControllerFactory', () => {
    describe('getController returns proper controller with dependencies', async () => {

    })
    
    describe('getController on non existing controller throws error', async () => {
        
    })
})