import "reflect-metadata"
import 'mocha'
import * as chai from 'chai'
import { setupChai } from './utils/utils'

const expect = setupChai(chai)
describe('RepositoryFactory', () => {
    describe('getRepository returns proper repository', async () => {

    })
    
    describe('getRepository on non existing repository throws error', async () => {
        
    })
})